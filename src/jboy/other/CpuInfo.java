package jboy.other;

import jboy.system.CPU;

public class CpuInfo {
    private CPU cpu;

    public CpuInfo(CPU cpu) {
        this.cpu = cpu;
    }

    public CPU getCpu() {
        return cpu;
    }
}
